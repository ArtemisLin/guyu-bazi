{
  "name": "@bazi/plugin",
  "private": true,
  "license": "AGPL-3.0-only",
  "version": "0.1.20",
  "type": "module",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "dev": "node esbuild.config.mjs --watch",
    "smoke": "node tools/smoke.mjs",
    "rollback": "node esbuild.config.mjs --rollback",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "release": "node tools/release.mjs"
  },
  "dependencies": {
    "@bazi/core": "*",
    "@bazi/view": "*"
  },
  "devDependencies": {
    "esbuild": "0.21.5",
    "obsidian": "^1.7.2",
    "happy-dom": "^20.11.1"
  }
}
